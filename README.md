# model-training-and-evaluation
This project trains and evaluates two machine learning models on the same dataset. Model performance is compared using test accuracy and training curves. The trained model is also tested on sample images, where predicted labels correctly match visual content.
